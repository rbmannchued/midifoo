# DSP Algorithm Tests

Compares the actual main frequency of the pre recorded sample with the DSP algorithm.

```
make
make run
```
You can use ``.m`` files to confirm sample frequency peaks and plot response.